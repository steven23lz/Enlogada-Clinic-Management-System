# Requirement Traceability Matrix

Living document. One row per **feature** (a module may have many features). No row may exist without a MODULE value from `MODULE_SCOPE.md`'s approved 18. This file is what lets Project Architect confirm "implemented" means "traced end-to-end," not just "code exists" — and it's the audit trail that makes silent scope creep (including any reintroduction of excluded functionality) structurally visible.

## Chain

```
MODULE → FEATURE → USER STORY → ACCEPTANCE CRITERIA → UI → API → DATABASE → SECURITY → ACCESSIBILITY → TEST → REVIEW → APPROVAL
```

*(Revised 2026-08-10: SECURITY and ACCESSIBILITY were previously bundled into one `VALIDATION` column, and REVIEW/APPROVAL into one `IMPLEMENTATION STATUS` column. They are now separate columns so a feature's status can't hide "security passed but accessibility didn't," or "code-reviewed but not yet Project-Architect-approved," behind a single combined cell.)*

## Column ownership and write access

Per `AGENTS.md`'s File Ownership Model, `TRACEABILITY.md` lives under `.agents/**`, which defaults to Project Architect ownership. The grants below are the **standing controlled exception** for this file: each listed agent may write to *its own column(s) only*, without asking Project Architect each time, because the change is necessary for their assigned task and the scope is explicitly documented here (satisfying all three conditions in the File Ownership Model). Writing to a column you don't own, or restructuring this file's chain/format, still requires Project Architect approval.

| Column | Owning agent(s) | Notes |
|---|---|---|
| MODULE | Feature/Module Architect | Must be one of the 18 in `MODULE_SCOPE.md`. No exceptions without escalation. |
| FEATURE | Feature/Module Architect | A named, bounded slice of the module. |
| USER STORY | Business Analyst | Standard "As a [role], I want [x], so that [y]" form. |
| ACCEPTANCE CRITERIA | Business Analyst | Testable, specific conditions of done. |
| UI | UX/UI Design Lead (spec reference) + Frontend Engineer (implementation reference) | Both write to this column, at different stages. |
| API | Backend Engineer | Endpoint(s) touched or added. |
| DATABASE | Database Engineer | Table(s)/column(s) touched or added. |
| SECURITY | Security Engineer | RBAC/auth/ownership review sign-off or blocking findings. |
| ACCESSIBILITY | Accessibility Specialist | WCAG sign-off or blocking findings (`a11y-audit`). |
| TEST | QA/Test Engineer | Playwright test file/case reference (see `frontend/tests/e2e/`). |
| REVIEW | Code Reviewer (mechanical/technical verdict) + UI/UX Auditor (holistic UX/consistency verdict) | Both write to this column — see `AGENTS.md` for why these are distinct, non-duplicating reviews. |
| APPROVAL | Project Architect | Final status: `Not started` / `In progress` / `Review` / `Approved` / `Escalated — out of scope`. |

## Table

Rows are added only when a feature enters Module Decomposition. First rows opened 2026-08-10 for Module 1.

| MODULE | FEATURE | USER STORY | ACCEPTANCE CRITERIA | UI | API | DATABASE | SECURITY | ACCESSIBILITY | TEST | REVIEW | APPROVAL |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Authentication | Register | As a visitor, I want to create an account, so that I can book appointments as a Client | Valid data creates a Client account; duplicate email rejected | `Register.jsx` (pre-existing) | `POST /auth/register` (pre-existing) | `users`, `user_roles` (pre-existing) | Clean — no findings | Clean — no findings | `smoke.spec.js`, `auth.spec.js` (indirect via login) | Pre-existing, re-verified this pass | Approved |
| Authentication | Login (email/password) | As a registered user, I want to sign in with email/password, so that I can reach my dashboard | Valid credentials issue a JWT with correct roles/permissions; invalid credentials rejected without revealing which field was wrong | `Login.jsx` (pre-existing) | `POST /auth/login` (pre-existing) | `users`, `user_roles`, `role_permissions` (pre-existing) | Clean — no findings | Clean — no findings | `auth.spec.js`, `api-authorization.spec.js` (login used as setup in both) | Pre-existing, re-verified this pass | Approved |
| Authentication | Google Sign-In | As a visitor, I want to sign in with Google, so that I don't need a separate password | Valid Google ID token logs in or auto-creates a Client account | `Login.jsx` `GoogleLogin` (pre-existing) | `POST /auth/google` (pre-existing) | `users`, `user_roles` (pre-existing) | Known gap unchanged by this pass: such accounts get a random password — now recoverable via Password Reset (see below) | Not independently re-audited this pass (third-party widget) | Not covered (requires a real Google token; no automated coverage) | Pre-existing, not re-reviewed line-by-line this pass | Approved (pre-existing scope) |
| Authentication | Session (JWT) | As a logged-in user, I want my session to persist across reloads, so that I stay logged in | Token in localStorage restores session via `GET /auth/me`; 401 clears session app-wide | `AuthContext.jsx` (pre-existing) | `GET /auth/me` (pre-existing) | — | Clean — no findings | Clean — no findings | Exercised implicitly by every authenticated test | Pre-existing, re-verified this pass | Approved |
| Authentication | Logout | As a logged-in user, I want to log out, so that my session ends on this device | Logout clears token and returns to public view | `Navbar.jsx`/`SidebarLayout.jsx` (pre-existing) | client-side only (pre-existing) | — | Clean — no findings | Clean — no findings | `auth.spec.js` "logout returns to the public view" | Pre-existing, re-verified this pass | Approved |
| Home | Public landing page | As a visitor, I want to see the clinic's introduction, service highlights, and clear calls to action, so that I can decide to book or browse services | Hero, 3 highlight cards, and 2 CTAs render; both CTAs navigate correctly for an anonymous visitor | `Home.jsx` (pre-existing content; 3 color-drift instances fixed this pass — see UI/UX below) | None (fully static) | None | Clean — fully static public page, no new attack surface. Informational note: hero image is hotlinked to `images.unsplash.com` (pre-existing, third-party dependency + minor referrer/privacy consideration) | Clean — no blocking findings; decorative icon `aria-hidden` gap noted as a pre-existing app-wide pattern, not fixed in isolation | `home.spec.js` (7 tests) | Reviewed — 3-line color-token fix only, no logic change, no layering concerns | Approved |
| Patient Management | Edit own patient profile (new UI; create/view pre-existing) | As a client, I want to edit a patient profile's details, so that I can correct or update information without contacting staff | Edit form pre-fills current data; save persists changes; a client cannot edit another client's patient profile | `ClientDashboard.jsx` "Edit Patient Profile" dialog (new — mirrors the pre-existing Add dialog exactly) | `PUT /patients/:id` (pre-existing, already had the correct ownership check — no backend change needed) | No schema change — reused `patients` | Reviewed — confirmed the pre-existing ownership check actually works via a live IDOR regression test; zero backend code touched, so no new attack surface | Reviewed — added `role="alert"` to this dialog's error banner and, since it's the same module, to the adjacent pre-existing Add-Profile dialog's banner too (same gap, same file) | `patient-management.spec.js` (5 tests, incl. IDOR regression) | Reviewed — pure frontend addition, mirrors existing Add-Profile pattern exactly, no duplication beyond the intentional parallel-dialog structure | Approved |
| Appointment | View/Cancel own appointments (new UI; booking + availability pre-existing) | As a client, I want to see my booked appointments and cancel one if needed, so that I'm not stuck with a booking I can't change | "My Appointments" list shows date/time/reference/status; Cancel available only for Pending/Confirmed; a client cannot view or cancel another client's appointment | `ClientDashboard.jsx` "My Appointments" card (new, adopts `StatusBadge`/`ConfirmDialog`) | `GET /appointments/my-bookings` (pre-existing, now used), `PUT /appointments/:id/cancel` (ownership check added), `POST /appointments` (ownership check added) | No schema change — reused `appointments`/`patient_visits` | Reviewed — fixed 2 real IDOR gaps found during inspection: `cancelAppointment` ignored the `userId` it was passed; `createAppointment` never verified caller owns `patientId`. Both fixed with a shared `assertClientOwnsPatient` check, regression-tested. Also: live dev DB was found missing `clinic_operating_hours` entirely (unrelated to this module's code, cause unconfirmed) — restored additively; see Module 3 report | Clean — text-based status (not color-only), no icon-only controls, Radix Dialog handles focus/ARIA | `appointments.spec.js` (7 tests, incl. both IDOR regressions) | Reviewed — shared ownership-check helper avoids duplication, layering intact, no dead code; 2 self-verification bugs (date display, missing table) found and fixed before this point | Approved |
| Authentication | **Password Reset** (new) | As a user who forgot their password, I want to request a reset link by email and set a new password | No account enumeration; link single-use, expires in 1hr; Google-only accounts gain a real usable password | `ForgotPassword.jsx`, `ResetPassword.jsx` (new — color-consistency fix applied during UI/UX Audit) | `POST /auth/forgot-password`, `POST /auth/reset-password` (new) | `password_reset_tokens` (new, `migrations.md` [1.3.0]) | Reviewed — see findings below; all P0/P1 clear, 3 documented residual risks (P2/P3, accepted) | `role="alert"`/`role="status"` added during review; label-association gap noted as pre-existing app-wide, not fixed in isolation | `password-reset.spec.js` (9 tests) + one-time manual full-happy-path verification (see Module 1 report) | Reviewed — layering/reuse/naming clean, no dead code | Approved |

| Profile | Own account settings (contact info, password, view role/permissions) | As a client, I want to update my contact info, change my password, and see my assigned role, so that I can keep my account current and understand what access I have | Contact info edits persist; wrong current password is rejected without changing anything; new password logs in and the old one no longer does; role/permissions render read-only from the JWT-backed profile | `ClientProfile.jsx` (new page) + `Navbar.jsx`/`DashboardLayout.jsx` (new "My Account" nav link, previously dead) | `PUT /auth/me`, `PUT /auth/change-password` (new) | No schema change — reused `users` | Reviewed — both endpoints derive the target user exclusively from the JWT (`req.user.userId`), never from body/params, so there is no IDOR surface by construction (regression-tested: a `userId` in the request body is silently ignored); password change requires the current password via bcrypt before accepting a new one. Accepted residual risk, consistent with the existing Password Reset module: no token revocation exists app-wide, so a JWT issued before a password change remains valid until it expires | Reviewed — `role="alert"`/`role="status"` banners matching the established pattern; Navbar user badge converted from a non-interactive `div` to a real labeled `button`. Label-to-input association gap is the same pre-existing app-wide pattern already noted in Modules 3/4, not fixed in isolation here | `profile.spec.js` (9 tests, incl. the userId-ignored regression) | Reviewed — layering intact (routes→controllers→services→repositories), reused existing bcrypt/JWT patterns from `authService`, no duplication | Approved |

| Diagnostic Result Viewing | Client's read-only diagnostic history + certificate report (pre-existing UI; 3 correctness/security fixes made this pass) | As a client, I want to see my own diagnostic test history and view/print released results, so that I can access my reports without contacting the clinic | Pending tests show status with no findings/attachment; Completed tests show findings/remarks and, only for a safe URL, a downloadable attachment link; all 5 real test categories are filterable; no React console key warnings when the list renders | `ClientDashboard.jsx` "Diagnostic History" + "View Certificate Report" dialog (pre-existing; fixed in place — see SECURITY/REVIEW) | `GET /results/history/:patientId` (pre-existing, unchanged — ownership check already correct) | No schema change — reused `visit_tests`/`test_results` | Reviewed — `test_results.file_url` is staff-entered free text with no format validation anywhere in the upload pipeline (`resultController.uploadResult`) and was rendered directly as an `<a href>`; a `javascript:` URI would have executed in the client's session. Fixed on the render side with an `isSafeResultUrl` allowlist (http/https/root-relative only); anything else now renders as plain "Attachment link unavailable" text instead of a link. Live-verified with an actual `javascript:` payload uploaded through the real staff endpoint. Ownership/IDOR on the history endpoint itself was already correct and already regression-tested in `api-authorization.spec.js` — not re-touched | Reviewed — no new icon-only controls; filter tabs are text-labeled buttons | `diagnostic-results.spec.js` (3 tests, incl. the malicious-file_url render test) | Reviewed — fixed the `key={item.id}` bug flagged (and deliberately deferred) back in Module 3: the API response has no `id` field, only `visit_test_id`, so React was silently falling back to index keys; also fixed the category filter tabs only covering 3 of the 5 seeded `test_categories` rows (Laboratory/Ultrasound/Xray present, 2D Echo/ECG missing — added, with a `CATEGORY_ICONS` lookup replacing the old ternary chain that mis-iconed both) | Approved |

| Receptionist | Patient record lookup (new) | As a receptionist, I want to search for an existing patient before registering a walk-in, so that returning patients aren't duplicated in the system | Searching by name finds existing patients (2+ char query required); selecting a match checks them in directly via a new visit, with no re-registration | `ReceptionistDashboard.jsx` "Find Existing Patient" card (new, in the Walk-In tab, above the registration form) | `GET /patients/search` (new) | No schema change — reused `patients` | Reviewed — staff-only (`SuperAdmin`/`Admin`/`Receptionist`), regression-tested that a Client role gets 403; parameterized `ILIKE` query, no injection surface; route registered before `/:id` to avoid Express matching "search" as an id param | Reviewed — no new icon-only controls; search result rows are plain text + labeled button | `receptionist.spec.js` (part of 9 tests) | Reviewed — reuses the existing walk-in visit-creation call path, no duplication | Approved |
| Receptionist | HMO request initiation (existing logic wired up; route bug fixed) | As a receptionist, I want to log an HMO pre-authorization request against a visit's diagnostic test, so that the approval process can start before results are billed | Selecting an HMO provider (and optionally an approval code) for an attached test logs the request; the request is retrievable for later approval (module 15's existing approve endpoint, untouched) | `ReceptionistDashboard.jsx` HMO modal (new — the trigger button and Dialog JSX had never been built; the state/handler already existed, unused, since before this pass) | `POST /hmo/request` (pre-existing; call site fixed from a nonexistent `/hmo/requests`) | No schema change — reused `hmo_requests`/`hmo_request_tests` | Reviewed — found and fixed a route-path bug: the frontend called `POST /hmo/requests` (plural) against a backend that only registers `POST /hmo/request` (singular), a silent 404. Fixed in both call sites: this module's `ReceptionistDashboard.jsx` and Module 3's `ClientDashboard.jsx` booking flow, which had carried the identical bug since Module 3 was approved (its test fixtures never exercised the optional HMO step, so it went uncaught). No Module 15-owned backend file (`hmoRoutes.js`/`hmoController.js`) was touched — the existing route was already correct, only the callers were wrong | Reviewed — error/success banners use `role="alert"`/`role="status"`; new HMO trigger buttons have descriptive `aria-label`s | `receptionist.spec.js` (incl. a regression test asserting the old plural path now correctly 404s) | Reviewed — reused the pre-existing `handleHmoSubmit`/state wholesale, only added the missing trigger and Dialog markup | Approved |

| Cashier | Billing checkout & receipt (pre-existing UI; double-billing gap fixed this pass) | As a cashier, I want to bill a visit's diagnostic tests and issue a receipt, so that a patient can proceed to diagnostics once paid | Bill total matches attached tests' priced items; payment methods (Cash/GCash/PayMaya/Bank) validate correctly; a receipt is issued and logged; an already-paid visit can no longer be selected or billed again | `CashierDashboard.jsx` (pre-existing; queue-filtering fix added this pass) | `GET /payments/bill/:visitId`, `POST /payments`, `GET /payments/transactions` (all pre-existing, unchanged) | No schema change — reused `payments` | Reviewed — found and fixed a double-billing gap: `GET /visits/active` returns both `Pending` and `Processing` visits, and a visit stays `Processing` after payment, so a paid visit remained fully selectable and payable again with zero protection. Fixed entirely within this module's own file by cross-referencing `payments.payment_status === 'Paid'` from data already fetched (`transactions`) — no backend/schema change. Two deeper gaps found in the process are Module 14's, not this module's, to fix — see the two escalation rows immediately below | Reviewed — payment-method buttons and cash presets are text-labeled, not icon-only | `cashier.spec.js` (5 tests, incl. the double-billing regression) | Reviewed — reused already-fetched state, no new API calls, matches the file's existing `alert()` convention for this class of guard | Approved |

| `OUT OF SCOPE` (Module 14: Payment) | `paymentService.getBillingSummary` grants 100% "HMO Coverage" purely from `patient_type_name === 'HMO'` (a billing category set at patient registration), with zero cross-check against whether an actual `hmo_requests`/`hmo_request_tests` record was ever approved for that visit's tests (Module 15's workflow, built in Module 7). Any patient registered under the HMO billing category currently receives free services at checkout regardless of real authorization status. | — | Escalated — awaiting Project Architect decision. Requires changes to `paymentService.js` (Module 14) cross-referencing `hmo_requests`/`hmo_request_tests` (Module 15) — both out of bounds for Modules 6–8's scope, found during Module 8 (Cashier) inspection |
| `OUT OF SCOPE` (Module 14: Payment) | `paymentController.processPayment` trusts the client-submitted `amount` outright rather than recomputing/verifying it against the server-side bill total, and has no idempotency guard — a second `POST /payments` for an already-paid `patientVisitId` is accepted with no error (confirmed live during Module 8 testing). Module 8's own UI now prevents this in the normal flow (see the Cashier row above), but the authoritative server-side guard is missing. | — | Escalated — awaiting Project Architect decision. Requires changes to `paymentService.js`/`paymentController.js` (Module 14), found during Module 8 (Cashier) inspection |

| Laboratory Staff | Laboratory result entry, review & release (pre-existing UI; release-notification gap fixed this pass) | As a Laboratory Staff member, I want to record findings for a test and release the result, so that the patient is notified their results are ready | Starting a pending test moves it to Processing; recording findings and confirming release marks it Completed, notifies the patient (if their account has a linked email), and removes it from the pending worklist; an unsafe attachment URL is rejected before submission | `DiagnosticDashboard.jsx` (pre-existing worklist/template UI; release-chaining and URL validation added this pass) | `GET /results/pending/:category`, `PATCH /results/test-status/:visitTestId`, `POST /results/:visitTestId`, `POST /results/:visitTestId/release` (all pre-existing, unchanged) | No schema change — reused `visit_tests`/`test_results` | Reviewed — found and fixed: `POST /results/:visitTestId/release` (the actual release step that sends the patient email notification) was never called anywhere in the frontend; "Authorize & Release Result" only recorded findings. Fixed by chaining the release call after a successful upload, entirely within this module's own file — the endpoint itself was already correct and already role-gated. Also added client-side validation on the optional attachment URL (rejects non-http(s) values before submission), the source-side complement to the Module 6 render-side guard. A deeper finding is NOT this module's to fix — see the escalation row immediately below | Reviewed — added `role="alert"` to the upload-error banner (same gap/fix pattern as Modules 1/4/5); template buttons and status badges are text-labeled, not icon/color-only | `laboratory.spec.js` (7 tests) | Reviewed — release-chaining reuses the pre-existing endpoint as-is; URL validation is a small pure function, no duplication | Approved |

| `OUT OF SCOPE` (Module 16: Diagnostic Result) | `resultController.js`/`resultService.js` authorize *any* diagnostic staff role (Laboratory/Xray/Ultrasound Staff) for *any* category's `pending`, `test-status`, result-upload, and `release` actions — there is no check that a Laboratory Staff account calling these endpoints directly is only acting on Laboratory-category `visit_tests`. The UI-level category scoping (`DiagnosticDashboard.jsx` picking a category from the caller's role) is purely client-side convenience with no server-side enforcement; confirmed live that the only role check is "is this caller some kind of diagnostic staff," not "does this visit_test's category match their department." | — | Escalated — awaiting Project Architect decision. Requires changes to `resultController.js`/`resultService.js` (Module 16), which would apply symmetrically to Modules 10/11 as well as this module — found during Module 9 (Laboratory Staff) inspection |

| Ultrasound Staff | Ultrasound (incl. 2D Echo) result entry, review & release | As an Ultrasound Staff member, I want to see and process both Ultrasound and 2D Echo tests in one worklist, so that I don't miss the 2D Echo tests my role is also responsible for | The worklist shows tests from both categories with correct per-row labels; recording findings and releasing removes the test from the worklist regardless of which of the two categories it belongs to | `DiagnosticDashboard.jsx` (shared with Modules 9/11; merge logic and a race-condition fix added this pass) | `GET /results/pending/:category` (whitelist extended), `PATCH /results/test-status/:visitTestId`, `POST /results/:visitTestId`, `POST /results/:visitTestId/release` (all otherwise unchanged, inherited working from Module 9) | No schema change — reused `visit_tests`/`test_results`/`test_categories` | Reviewed — found that `'2D Echo'` (a distinct `test_categories` row) was hard-rejected by the worklist-visibility whitelist, meaning no staff member could ever discover a pending 2D Echo test through any worklist, even though entry/release themselves had no such restriction. Per explicit user decision, fixed with a narrow, mechanical whitelist addition in `resultService.js` (Module 16) — confirmed this did not touch the cross-category authorization gap escalated in Module 9, and confirmed Laboratory's worklist is unaffected. Fixing this also surfaced and fixed a real, separate bug: the component fetched once immediately with a hardcoded default category, then again once the user's role resolved — a last-response-wins race that the new, slower merged Ultrasound+2D Echo fetch made practically triggerable (reproduced live, then fixed by not fetching until the real category is resolved) | Reviewed — no new icon-only controls; the "(incl. 2D Echo)" label change is plain text | `ultrasound.spec.js` (6 tests) | Reviewed — reused Module 9's fetch/entry/release logic as-is; the category merge and resolution-gating are both minimal, targeted changes | Approved |

| `OUT OF SCOPE` (spans Modules 9/10/11 + 16) | Real file upload does not exist anywhere in this application, for any diagnostic test category — confirmed no upload library (e.g. `multer`) is even installed. `test_results.file_url` is a plain free-text URL field staff must manually type/paste; there is no file picker, no server-side storage, and no mechanism to actually attach a scan/report file. Per explicit user decision during Module 10, this is out of scope for the current module-by-module pass and needs a dedicated design pass (storage choice, upload endpoint, validation, UI) spanning the shared diagnostic-entry form and Module 16's backend. | — | Escalated — awaiting Project Architect / user decision on scheduling a dedicated file-upload implementation pass. Found during Module 10 (Ultrasound Staff) inspection |

| X-ray Staff | Digital X-Ray-category result entry, review & release | As an X-ray Staff member, I want to record findings for a Chest/Abdominal X-Ray test and release the result, so that the patient is notified their results are ready | Starting a pending test moves it to Processing; recording findings (including via the Xray-specific "Normal Chest X-Ray" template) and confirming release marks it Completed, notifies the patient, and removes it from the pending worklist | `DiagnosticDashboard.jsx` (shared with Modules 9/10; zero changes needed this pass — see SECURITY) | `GET /results/pending/Xray`, `PATCH /results/test-status/:visitTestId`, `POST /results/:visitTestId`, `POST /results/:visitTestId/release` (all pre-existing, unchanged) | No schema change | Reviewed — inspection plus live end-to-end verification found this module needed **zero application code changes**: Xray's category name was already correct in the worklist whitelist (unlike '2D Echo' in Module 10), and Modules 9/10 had already fixed every shared bug in `DiagnosticDashboard.jsx` (dead release call, missing URL validation, missing `role="alert"`, the category-resolution race). Confirmed live: full worklist→process→findings→release flow works, the `'Xray Staff'` role string (no hyphen, per this module's explicit `MODULE_SCOPE.md` callout) is used correctly and consistently everywhere it appears (`SidebarLayout.jsx`'s `roleRequired`, `determineCategory`, `App.jsx`'s role branch), and cross-category isolation from Laboratory's worklist holds | Reviewed — no new UI, nothing to review beyond what Modules 9/10 already covered | `xray.spec.js` (7 tests, dedicated coverage rather than relying solely on Modules 9/10's tests) | Reviewed — no code changes; added the missing `xray@enlogada.com` seeded test account (`seedUsers.js`, matching the existing `lab@enlogada.com`/`ultrasound@enlogada.com` pattern) so this module's tests run on any fresh clone | Approved |

| Admin Dashboard | Staff accounts, cashier monitoring, appointments/patient-records oversight, reporting entry point (services catalog was already complete via `ServicesCatalog.jsx`) | As an Admin/SuperAdmin, I want a console to manage operational staff accounts and oversee cashier activity, appointments, and patient records, so that I don't need direct database access to run daily clinic operations | Each of the 6 real admin nav destinations shows its own distinct, correct content (previously all 8 showed identical static content, `activeNav` was ignored entirely); staff accounts can be created (5 operational roles only, never Admin/SuperAdmin) and activated/deactivated; cashier/appointments/patient views show real, live data; the dashboard KPIs are computed from real data, not hardcoded | `AdminDashboard.jsx` rebuilt as a thin `activeNav` router; 5 new focused sub-views under `frontend/src/pages/admin/` (`StaffAccounts`, `CashierMonitoring`, `AppointmentsOversight`, `PatientRecordsOversight`, `ReportsOverview`); the pre-existing embedded catalog-editing table (duplicating `ServicesCatalog.jsx`) was removed as dead-weight duplication | New: `GET/POST /admin/staff`, `PATCH /admin/staff/:id/status`, `GET /appointments` (oversight). Reused, unchanged: `GET /payments/transactions`, `GET /patients/search`, `GET /tests`, `GET /rbac/matrix` | No schema change — reused `users`/`user_roles`/`appointments` | Reviewed — `adminService.js` restricts staff creation/status changes to the 5 operational roles only (`MANAGEABLE_ROLES` constant), explicitly rejecting Admin/SuperAdmin role requests server-side (not just hidden in the UI) — regression-tested. `updateStaffStatus` re-validates the target account actually holds a manageable role before allowing a status change, so the endpoint can't be pointed at a Client or Admin account — regression-tested. All new endpoints are Admin/SuperAdmin-gated; tested that Client and Receptionist both get 403 from each. `appointmentRepository.findAll`'s dynamic WHERE clause uses parameterized `$n` placeholders throughout, not string-interpolated values — no injection surface | Reviewed — new error banners use `role="alert"`; status/confirm toggles reuse the existing accessible `ConfirmDialog` pattern; no new icon-only controls | `admin-dashboard.spec.js` (17 tests) | Reviewed — eliminated a real duplication/consistency risk (two independent catalog editors for the same data) rather than adding a third; reused 3 existing endpoints instead of building new ones where existing authorization already fit; new backend follows the established routes→controllers→services→repositories layering. Found and fixed a real trust bug: the "Today's Revenue ₱4,850.00 / +14% vs yesterday" KPI was hardcoded fake data in a financial admin dashboard, now computed live from `GET /payments/transactions` with a genuine yesterday comparison; the "8 Roles" KPI was hardcoded-but-accidentally-correct, now fetched live from the RBAC matrix so it can't silently go stale. The `service-requests` nav item (pre-existing, not named in this module's own scope) was left as an honest "not yet available" placeholder rather than building Module 15's functionality under it | Approved |

| Super Admin Management | RBAC administration (matrix viewer/editor) + elevated (Admin/SuperAdmin) account management | As a SuperAdmin, I want to view and edit the role-permission matrix and manage Admin/SuperAdmin accounts, so that I have oversight and control Admin cannot have | The matrix displays all 8 roles with their assigned permissions and can be edited per role; Admin can still view it but is rejected from editing; elevated accounts can be created/activated/deactivated by SuperAdmin only, with a guard against deactivating one's own account | `SuperAdminManagement.jsx` (new — Role-Permission Matrix + Elevated Accounts tabs), new SuperAdmin-only nav item in `SidebarLayout.jsx` (first nav item in the app gated to SuperAdmin alone, not paired with Admin) | New: `GET/POST /superadmin/accounts`, `PATCH /superadmin/accounts/:id/status`. Refactored: `GET /rbac/matrix` (unchanged authorization), `PUT /rbac/roles/:roleId/permissions` (tightened from SuperAdmin+Admin to SuperAdmin-only) | No schema change — reused `users`/`user_roles`/`roles`/`permissions`/`role_permissions` | Reviewed — this is the first module to actually distinguish SuperAdmin from Admin anywhere in the app (every prior route paired them). Found and fixed two real bugs in the pre-existing `rbacController.js` while refactoring it into proper routes→controller→service→repository layers (it previously ran raw SQL directly in the controller, violating this repo's own layering convention): (1) `updateRolePermissions` deleted a role's permissions then re-inserted one at a time with no transaction — a failure partway through could silently leave a role with zero permissions; now wrapped in BEGIN/COMMIT/ROLLBACK, regression-tested for consistency. (2) The edit endpoint was callable by Admin, not just SuperAdmin, backwards for the one capability this module explicitly owns; tightened and regression-tested (Admin still gets 200 on view, 403 on edit). New elevated-account endpoints are SuperAdmin-only, tested against Admin/Client/unauthenticated all correctly rejected; account creation validates against an explicit role whitelist (rejects non-elevated roles); status updates re-validate the target is actually elevated (rejects targeting a Client); a self-deactivation guard prevents a SuperAdmin from locking the clinic out of its own administration, regression-tested live | Reviewed — new error banners use `role="alert"`; checkbox permission toggles use native `<label>`-wrapped `<input>` pairs; status badges reuse the established clickable-badge-with-confirm pattern from `ServicesCatalog.jsx`/`StaffAccounts.jsx`, disabled and visually muted for one's own account | `super-admin.spec.js` (16 tests) | Reviewed — new backend follows the established layering convention exactly (the refactor fixed a prior violation rather than adding a new one); `superAdminService.js` deliberately mirrors, rather than modifies, Module 12's `adminService.js` pattern to avoid touching an already-approved module's file; reused `userRepository.findStaffUsers`/`updateUserStatus` (both already role-agnostic) rather than duplicating | Approved |

| Payment | Payment records/billing status data layer + client-side payment visibility (fixes 2 gaps escalated from Module 8) | As a client, I want to see my own payment history, and as the clinic, I want HMO coverage and payment amounts to be trustworthy, so that billing can't be gamed and clients have visibility into what they've paid | HMO coverage reflects only actually-approved per-test authorizations, never a blanket category assumption; a second payment for an already-paid visit is rejected; a tampered payment amount is rejected; a client sees their own payment history across all their patient profiles, never another client's | `ClientDashboard.jsx` new "Payment History" card (mirrors the existing "My Appointments" card pattern) | `paymentService.js`/`paymentController.js`/`paymentRepository.js` (Module 14's own files) — `getBillingSummary` HMO logic rewritten, `processPayment` given idempotency + amount-verification guards; new `GET /payments/my-payments` (self-scoped, no role restriction beyond being logged in, matching `/patients/my-profiles`/`/appointments/my-bookings`) | No schema change — reused `payments`/`hmo_request_tests` | Reviewed — both fixes were formally escalated (not fixed) during Module 8 specifically because they belong here; now resolved. (1) HMO coverage previously granted 100% from `patient_type_name === 'HMO'` alone; now computed per test from `hmo_request_tests.approval_status` (Module 15's data, read-only) — regression-tested for zero/partial/full coverage, and specifically tested that a request-level approval alone (without the separate per-test approval step) still yields zero coverage, since those are genuinely different fields in this schema. Used a correlated subquery rather than a LEFT JOIN for this lookup — a visit_test could in principle link to more than one hmo_request_tests row, and a JOIN would have duplicated that line item, silently inflating the bill subtotal (caught and fixed before testing, not shipped). (2) `processPayment` now recomputes the authoritative bill server-side (also serving as the visit-exists check) and rejects a mismatched submitted amount with 400, and rejects a second payment for an already-`Paid` visit with 409 — both regression-tested; confirmed live during Module 8 that the old behavior silently accepted a duplicate payment. Accepted residual risk: the duplicate-payment check is read-then-insert with no row lock, so a narrow TOCTOU race remains under truly concurrent requests — consistent with this codebase's existing, never-hardened tolerance for the same race shape in `receipt_number`/`queue_number` generation, and already mitigated in the normal flow by Module 8's UI-side queue filter. New `/payments/my-payments` endpoint is self-scoped via the JWT only, regression-tested that a client with no payments gets an empty list rather than another client's data | Reviewed — Payment History card reuses `StatusBadge` and the established card/list pattern, no new interactive elements (read-only) | `payment.spec.js` (11 tests) | Reviewed — `findPaymentsByPatientUserId` mirrors the existing `findByPatientUserId`/`findPatientsByUserId` pattern exactly; no duplication; layering intact | Approved |

| Test and Service Request | visit_tests attachment ownership fix + HMO request/approval flow (approval half was entirely missing) | As a client, I want my own visit's tests kept private from other clients, and as an admin, I want to discover and approve pending HMO requests, so that billing and coverage decisions are auditable and safe | A client cannot attach tests to another client's visit; staff are unaffected; all pending HMO requests are listable and filterable by status; an admin can approve a request and independently approve/reject each of its linked tests | New `frontend/src/pages/admin/ServiceRequests.jsx` (replaces the honest placeholder left in Module 12); `CashierDashboard.jsx` HMO banner corrected (see SECURITY note) | New: `GET /hmo/requests`. Fixed: `POST /tests/visit-tests` (ownership check added). Unchanged: `PUT /hmo/request/:id/approve`, `PUT /hmo/request-test/:id` (already existed, now reachable through a real UI for the first time) | No schema change — reused `visit_tests`/`hmo_requests`/`hmo_request_tests` | Reviewed — fixed a known IDOR gap flagged during the pre-implementation remediation pass and deliberately deferred through every module since: `testService.addTestsToVisit` performed zero ownership check despite `POST /tests/visit-tests` authorizing the Client role, so any client could attach (and be billed for) tests on an arbitrary `patientVisitId`. Fixed with the same `assertClientOwnsPatient`-shaped guard already established in `appointmentService.js`; regression-tested that a client is blocked from another client's visit, allowed on their own, and that staff callers are unaffected (not ownership-restricted). Also, per explicit user approval, fixed a direct consequence of Module 14's own fix: `CashierDashboard.jsx` had a banner unconditionally claiming "Full coverage approved (₱0.00 Out of Pocket)" for any HMO-category patient regardless of real approval — now reflects the actual computed `hmoCoverage`, with a distinct honest state for zero-coverage-yet | Reviewed — new `Check`/`X` per-test approval buttons have descriptive `aria-label`s; error banners use `role="alert"`; `StatusBadge` reused for both request- and test-level status display | `service-requests.spec.js` (12 tests); updated one now-obsolete Module 12 test in `admin-dashboard.spec.js` that asserted the old placeholder text, since this module legitimately replaced it | Reviewed — `assertClientOwnsVisit` mirrors the established ownership-check pattern exactly rather than inventing a new shape; `findAllRequests` reuses the existing `hmo_request_tests` join already used elsewhere; no duplication | Approved |

| Diagnostic Result | The shared `test_results` data/logic layer (fixes 1 cross-category authorization gap escalated from Module 9; 1 additional retry-safety gap found this pass) | As diagnostic staff, I want the result-entry/release endpoints to only let me act on my own department's tests, and as the clinic, I want a failed release notification to be safely retryable, so that departments stay isolated and a network hiccup can't permanently strand a result | A staff account can only list/advance/upload/release `visit_tests` in their own department's category (SuperAdmin/Admin exempt); acting on another department's category is rejected with 403; retrying the upload call after a prior successful upload (e.g. following a failed release call) overwrites cleanly instead of erroring | — (no UI in this module's own scope; consumed read-only by Modules 6/9/10/11, all previously built/fixed in their own passes) | `GET /results/pending/:category`, `PATCH /results/test-status/:visitTestId`, `POST /results/:visitTestId`, `POST /results/:visitTestId/release` (all pre-existing, unchanged surface — authorization tightened inside `resultService.js`) | No schema change — reused `test_results`/`visit_tests`/`tests`/`test_categories`; `resultRepository.createResult` changed from a plain `INSERT` to an upsert (`ON CONFLICT (visit_test_id) DO UPDATE`), using the pre-existing `UNIQUE` constraint on `visit_test_id` | Reviewed — fixed the cross-category authorization gap formally escalated from Module 9 (see the `OUT OF SCOPE (Module 16)` row above): added `assertStaffOwnsVisitTest`/`assertStaffAllowedCategory` in `resultService.js`, which resolve a `visit_test`'s real category server-side and compare it against the caller's role-derived department (`Ultrasound Staff` covers both `Ultrasound` and `2D Echo`, matching Module 10's existing worklist merge); SuperAdmin/Admin bypass, matching the RBAC convention used everywhere else. Regression-tested: cross-department calls blocked with 403 on all 4 actions (pending-list, status-update, upload, release), same-department calls still succeed, SuperAdmin bypass still works regardless of category, and a nonexistent `visitTestId` returns 404 rather than a crash. Also found and fixed, not previously escalated: `createResult`'s plain `INSERT` against `visit_test_id`'s `UNIQUE` constraint made the mandatory two-call upload-then-release sequence (fixed in Module 9) non-retry-safe — if the release call failed after a successful upload, retrying the whole sequence would 500 on the constraint violation, permanently stranding that result un-released. Converted to an upsert; regression-tested that a retried upload for the same `visit_test` overwrites the findings cleanly and the subsequent release still succeeds. The separately-escalated real-file-upload gap (`OUT OF SCOPE`, spanning Modules 9/10/11+16, above) remains deliberately out of scope — no new decision reopened it this pass | N/A — no UI in this module's own scope | `diagnostic-result-layer.spec.js` (8 tests) | Reviewed — both fixes are small, contained changes inside this module's own files (`resultRepository.js`/`resultService.js`/`resultController.js`); the ownership check lives in the service layer, not the controller or route, keeping the routes→controllers→services→repositories layering intact; `assertStaffOwnsVisitTest`/`assertStaffAllowedCategory` are each defined once and reused across all 4 actions rather than duplicated per-endpoint; no schema change | Approved |

| Reporting | Clinic-wide metrics: revenue trend, service volume, operational (visit status), and RBAC matrix reports, all for an admin-selected date range (Module 12's "Today's Snapshot" entry point unchanged) | As an Admin/SuperAdmin, I want historical trends, a custom date range, and a role-permission matrix I can actually view, so that I can understand clinic performance over time, not just today | Selecting a date range and applying shows daily revenue, service volume by category, visit status breakdown, and payment method breakdown for that range; Admin can view (read-only) the same role-permission matrix SuperAdmin edits, even though no prior UI ever surfaced it to Admin | `ReportsOverview.jsx` rewritten as a tabbed page: pre-existing "Today's Snapshot" tab unchanged; new "Date-Range Reports" tab (lightweight CSS bar/line visuals, no new dependency) and new read-only "RBAC Matrix" tab | New: `GET /reports/summary?startDate=&endDate=` (`reportRepository.js`/`reportService.js`/`reportController.js`/`reportRoutes.js`, all new files). Reused, unchanged: `GET /rbac/matrix` (Module 13's endpoint — already Admin+SuperAdmin authorized, just never had an Admin-reachable UI before now) | No schema change — reused `payments`/`visit_tests`/`tests`/`test_categories`/`patient_visits` | Reviewed — `/reports/summary` is `authorizeRoles('SuperAdmin', 'Admin')`, matching the `/rbac/matrix`/`/admin/*` convention; regression-tested that Receptionist/Client both get 403 and unauthenticated gets 401. `reportService.assertValidRange` rejects missing dates, malformed (non-`YYYY-MM-DD`) dates, and `startDate` after `endDate`, all with 400 rather than letting a malformed value reach the database — regression-tested for all three. All 4 repository queries are parameterized (`$1`/`$2`), no injection surface. Found and fixed a real gap on inspection, not previously escalated: `GET /rbac/matrix` has always authorized Admin server-side, but the only UI that ever called it (`SuperAdminManagement.jsx`, Module 13) is gated to SuperAdmin alone in the sidebar nav, so Admin had zero UI path to a capability already granted to them — closed with a read-only matrix view here; regression-tested live that Admin (not SuperAdmin) can now reach it through Reports, while the Super Admin nav item itself, and the ability to edit the matrix, both remain exactly as Module 13 left them | Reviewed — bar/line chart segments carry `role="img"`/`aria-label` text equivalents (not color-only); the visit-status breakdown uses text-labeled badges, not color-only; date inputs reuse the existing `CashierMonitoring.jsx` date-range picker pattern | `reports.spec.js` (11 tests) — updated one now-obsolete Module 12 test in `admin-dashboard.spec.js` that asserted the old "reporting entry point" placeholder banner text, since this module legitimately replaced it with the new Date-Range Reports tab | Reviewed — new backend follows the established routes→controllers→services→repositories layering exactly; RBAC matrix report reuses the existing endpoint and data shape rather than duplicating `rbacRepository`'s queries; no chart library added, consistent with the codebase's existing minimalism | Approved |

| Notification | Real, per-user notification backend + UI, replacing `SidebarLayout.jsx`'s static 3-item mock; wired into 3 real trigger points matching MODULE_SCOPE.md's own named examples ("appointment/result/payment events") | As staff/admin, I want to be notified in-app when a new appointment is booked, a payment is processed, or a diagnostic result is released, so that I don't have to keep refreshing operational screens to notice new activity | The notification bell shows a real unread count (no more unconditional pulsing dot); opening it shows real, role-appropriate notifications with relative timestamps; clicking one or "Mark all read" persists via the backend; a client and unauthenticated caller are each rejected/scoped correctly | `SidebarLayout.jsx`'s notification dropdown rewritten to fetch real data, track unread count, and mark-as-read/mark-all-read, replacing the hardcoded array and always-pulsing dot | New: `GET /notifications`, `PATCH /notifications/:id/read`, `PATCH /notifications/read-all` (`notificationRepository.js`/`notificationService.js`/`notificationController.js`/`notificationRoutes.js`, all new). Integration: one `notificationService.notifyRoles(...)` call added to each of `appointmentService.createAppointment` (Module 3/7), `paymentService.processPayment` (Module 14), `resultService.releaseResult` (Module 16) | New `notification_events(id, title, message, type CHECK, created_at)` + `notification_reads(id, event_id → notification_events.id ON DELETE CASCADE, user_id → users.id ON DELETE CASCADE, is_read, UNIQUE(event_id, user_id))` + `idx_notification_reads_user`, `database/migrations.md` [1.4.0]→[1.5.0]. Split into two tables (event content vs. per-recipient read state) rather than the original single flat table, which duplicated title/message text once per broadcast recipient with no single place to correct it — event and read-state are genuinely different entities with different lifecycles (write-once vs. per-user-mutable). Refactor preserved the exact public JSON shape (`notificationRepository.js` aliases the join back to `id, user_id, title, message, type, is_read, created_at`), so `notificationService.js`, the controller, routes, and the frontend needed zero changes — verified by re-running `notifications.spec.js` unmodified post-refactor (10/10 still passing). Applied additively/replacively directly against the live dev database rather than via a full destructive `migrateDb.js` re-create, to avoid discarding the accumulated seed/test data from Modules 1–17 (the original `notifications` table held only disposable test rows from this module's own verification); `schema.sql` remains the canonical source for fresh installs | Reviewed — `GET/PATCH /notifications*` are self-scoped via the JWT only (`req.user.userId`), matching the `/payments/my-payments` convention; every query filters by `user_id`, so `markAsRead`'s `WHERE nr.id = $1 AND nr.user_id = $2` makes cross-user marking structurally impossible — regression-tested that SuperAdmin gets 404 (not information-leaking 403) attempting to mark a different Admin's own notification row as read. `notifyRoles` never throws (mirrors `config/email.js`'s `sendEmail` exactly — logs and swallows on failure) so a notification-layer bug can never break a real booking/payment/release operation; regression-tested all 3 trigger points fire for the correct broadcast roles only (e.g. booking notifies Receptionist+Admin+SuperAdmin but never Cashier; a processing Cashier or releasing Lab Staff member is never notified of their own action, only oversight roles are). Recipients are resolved via the pre-existing `userRepository.findStaffUsers`, not a new query, avoiding duplicated role-resolution logic. The event/reads split's insert is transaction-wrapped (`db.pool.connect()` + `BEGIN`/`COMMIT`/`ROLLBACK`, matching `rbacRepository.setRolePermissions`'s established pattern) so a broadcast can never leave an orphaned event with zero recipient rows | Reviewed — the bell button carries a dynamic `aria-label` reflecting the real unread count; unread items get a visible non-color-only indicator (a leading dot plus distinct background, not color alone); "Mark all read" and "Close" are real labeled buttons, not icon-only | `notifications.spec.js` (10 tests, incl. the 3 trigger points, ownership isolation, and the browser flow) | Reviewed — one shared, non-duplicated integration point (`notifyRoles`) is called from all 3 sites rather than each service growing its own ad hoc notification-creation logic; layering intact (routes→controllers→services→repositories); reused `userRepository.findStaffUsers` instead of a new role-resolution query; no schema change beyond the one new, purely additive table | Approved |

## Escalation row format

If a request arrives that does not trace to one of the 18 modules (including anything resembling excluded veterinary/pet functionality), it is recorded here instead of silently dropped or silently built:

| MODULE | FEATURE | ... | APPROVAL |
|---|---|---|---|
| `OUT OF SCOPE` | (brief description) | — | `Escalated — awaiting user/Project Architect decision` |

This keeps a visible record that the request was seen and deliberately not actioned, rather than it looking like it was simply forgotten.
